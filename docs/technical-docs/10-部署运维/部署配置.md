# 部署配置

<!--
文件名: 部署配置.md
路径: docs/technical-docs/10-部署运维/
功能: CAUC-SEP 部署配置指南
版本: v2.0
作者: CAUC-SEP Team
创建日期: 2024-03-15
最后更新: 2026-03-18
-->

## 概述

本文档详细说明 CAUC-SEP 自旋电子实验平台的生产环境部署配置，包括桌面应用部署、服务配置、安全加固等内容。

### 部署架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        CAUC-SEP 部署架构                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    Electron 桌面容器 (Node.js 20+)               │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │                 BrowserWindow (Chromium)                   │  │   │
│  │  │  ┌─────────────────────────────────────────────────────┐  │  │   │
│  │  │  │              Vue 3 前端应用                           │  │  │   │
│  │  │  │  Element Plus + ECharts + WebSocket Client          │  │  │   │
│  │  │  └─────────────────────────────────────────────────────┘  │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  │                              │                                   │   │
│  │                              │ IPC / HTTP                        │   │
│  │                              ▼                                   │   │
│  │  ┌───────────────────────────────────────────────────────────┐  │   │
│  │  │              FastAPI 后端 (Nuitka 编译)                    │  │   │
│  │  │  ┌─────────────────────────────────────────────────────┐  │  │   │
│  │  │  │  PyModbus + SQLAlchemy + NumPy + SciPy              │  │  │   │
│  │  │  │  设备驱动 | 数据分析 | WebSocket 推送                  │  │  │   │
│  │  │  └─────────────────────────────────────────────────────┘  │  │   │
│  │  └───────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              │                                          │
│                              ▼                                          │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                       数据存储层                                  │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │   │
│  │  │   SQLite    │  │    HDF5     │  │   文件存储   │             │   │
│  │  │  元数据DB   │  │  时序数据   │  │  导出文件    │             │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 部署方式对比

| 部署方式 | 适用场景 | 优势 | 劣势 |
|----------|----------|------|------|
| **桌面安装包** | 单机使用 | 开箱即用、无需配置、原生性能 | 不支持多用户 |
| **源码部署** | 开发测试 | 灵活配置、便于调试 | 需要手动配置环境 |

---

## 生产环境配置

### 环境变量配置

创建 `.env` 文件配置生产环境变量：

```bash
# .env
# ============================================================================
# CAUC-SEP 生产环境配置
# ============================================================================

# 应用环境
APP_ENV=production
LOG_LEVEL=INFO

# 数据库配置
DATABASE_URL=sqlite:///data/experiments.db

# 安全配置
SECRET_KEY=your-secret-key-here-change-in-production
CORS_ORIGINS=https://your-domain.com,https://another-domain.com

# JWT配置
JWT_SECRET_KEY=your-jwt-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# 硬件配置
SIMULATION_MODE=true
MOTOR_PORT=COM3
ELECTROMAGNET_PORT=COM4
AMMETER_PORT=COM5

# 文件存储
DATA_DIR=/app/data
LOGS_DIR=/app/logs
EXPORTS_DIR=/app/exports

# 性能配置
MAX_WORKERS=4
WORKER_TIMEOUT=120
```

### 后端配置文件

```python
# backend/config/settings.py
"""
CAUC-SEP 生产环境配置
"""

import os
from pathlib import Path
from pydantic_settings import BaseSettings
from typing import List


class Settings(BaseSettings):
    """应用配置"""
    
    APP_NAME: str = "CAUC-SEP"
    APP_VERSION: str = "3.5.0"
    APP_ENV: str = "production"
    DEBUG: bool = False
    
    HOST: str = "0.0.0.0"
    PORT: int = 8000
    WORKERS: int = 4
    
    DATABASE_URL: str = "sqlite:///data/experiments.db"
    
    SECRET_KEY: str = "change-this-in-production"
    CORS_ORIGINS: List[str] = ["http://localhost:8080"]
    
    JWT_SECRET_KEY: str = "change-this-in-production"
    JWT_ALGORITHM: str = "HS256"
    JWT_EXPIRATION_HOURS: int = 24
    
    LOG_LEVEL: str = "INFO"
    LOG_FILE: str = "logs/app.log"
    LOG_MAX_SIZE: int = 10 * 1024 * 1024
    LOG_BACKUP_COUNT: int = 5
    
    SIMULATION_MODE: bool = True
    MOTOR_PORT: str = "COM3"
    ELECTROMAGNET_PORT: str = "COM4"
    AMMETER_PORT: str = "COM5"
    
    DATA_DIR: Path = Path("data")
    LOGS_DIR: Path = Path("logs")
    EXPORTS_DIR: Path = Path("exports")
    
    MAX_WORKERS: int = 4
    WORKER_TIMEOUT: int = 120
    MAX_CONNECTIONS: int = 100
    
    class Config:
        env_file = ".env"
        env_file_encoding = "utf-8"


settings = Settings()
```

### Gunicorn 配置

```python
# backend/gunicorn.conf.py
"""
Gunicorn 生产环境配置
"""

import multiprocessing
import os

bind = f"0.0.0.0:{os.getenv('PORT', '8000')}"

workers = int(os.getenv("MAX_WORKERS", multiprocessing.cpu_count() * 2 + 1))
worker_class = "uvicorn.workers.UvicornWorker"
worker_connections = 1000
timeout = int(os.getenv("WORKER_TIMEOUT", "120"))
keepalive = 5

daemon = False
pidfile = "logs/gunicorn.pid"
user = None
group = None
tmp_upload_dir = None

accesslog = "logs/access.log"
errorlog = "logs/error.log"
loglevel = os.getenv("LOG_LEVEL", "info")
access_log_format = '%(h)s %(l)s %(u)s %(t)s "%(r)s" %(s)s %(b)s "%(f)s" "%(a)s" %(D)s'

limit_request_line = 4094
limit_request_fields = 100
limit_request_field_size = 8190

preload_app = True
reload = False
```

---

## 服务配置

### Systemd 服务配置 (Linux)

```ini
# /etc/systemd/system/cauc-sep.service
[Unit]
Description=CAUC-SEP Experiment Platform
After=network.target

[Service]
Type=simple
User=www-data
Group=www-data
WorkingDirectory=/opt/cauc-sep/backend
Environment="PATH=/opt/cauc-sep/venv/bin"
Environment="APP_ENV=production"
Environment="LOG_LEVEL=INFO"
ExecStart=/opt/cauc-sep/venv/bin/gunicorn main:app \
  --workers 4 \
  --worker-class uvicorn.workers.UvicornWorker \
  --bind 0.0.0.0:8000 \
  --timeout 120 \
  --access-logfile /var/log/cauc-sep/access.log \
  --error-logfile /var/log/cauc-sep/error.log

Restart=always
RestartSec=10
StandardOutput=syslog
StandardError=syslog
SyslogIdentifier=cauc-sep

LimitNOFILE=65535
LimitNPROC=4096

[Install]
WantedBy=multi-user.target
```

```bash
# 启动服务
sudo systemctl daemon-reload
sudo systemctl enable cauc-sep
sudo systemctl start cauc-sep

# 查看服务状态
sudo systemctl status cauc-sep

# 查看日志
sudo journalctl -u cauc-sep -f
```

### Windows 服务配置

使用 NSSM (Non-Sucking Service Manager) 将应用注册为 Windows 服务：

```batch
# 安装 NSSM
choco install nssm

# 创建服务
nssm install CAUC-SEP

# 配置服务参数
nssm set CAUC-SEP Application "C:\Program Files\CAUC-SEP\CAUC-SEP.exe"
nssm set CAUC-SEP AppDirectory "C:\Program Files\CAUC-SEP"
nssm set CAUC-SEP DisplayName "CAUC-SEP Experiment Platform"
nssm set CAUC-SEP Description "CAUC Spintronics Experiment Platform"
nssm set CAUC-SEP Start SERVICE_AUTO_START

# 启动服务
nssm start CAUC-SEP

# 停止服务
nssm stop CAUC-SEP

# 删除服务
nssm remove CAUC-SEP confirm
```

---

## 安全加固

### 网络安全

#### 防火墙配置

```bash
# Ubuntu/Debian (ufw)
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow ssh
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# CentOS/RHEL (firewalld)
sudo firewall-cmd --permanent --default-zone=public
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

#### SSL/TLS 配置

```nginx
# Nginx SSL 配置
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
}

server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

### 应用安全

#### 安全响应头

```python
# backend/middleware/security.py
"""
安全中间件
"""

from fastapi import Request
from starlette.middleware.base import BaseHTTPMiddleware


class SecurityHeadersMiddleware(BaseHTTPMiddleware):
    """添加安全响应头"""
    
    async def dispatch(self, request: Request, call_next):
        response = await call_next(request)
        
        response.headers["X-Frame-Options"] = "SAMEORIGIN"
        response.headers["X-Content-Type-Options"] = "nosniff"
        response.headers["X-XSS-Protection"] = "1; mode=block"
        response.headers["Referrer-Policy"] = "strict-origin-when-cross-origin"
        
        response.headers["Content-Security-Policy"] = (
            "default-src 'self'; "
            "script-src 'self' 'unsafe-inline' 'unsafe-eval'; "
            "style-src 'self' 'unsafe-inline'; "
            "img-src 'self' data: https:; "
            "font-src 'self' data:; "
            "connect-src 'self' ws: wss:;"
        )
        
        return response
```

#### 速率限制

```python
# backend/middleware/rate_limit.py
"""
速率限制中间件
"""

import time
from typing import Dict
from fastapi import Request, HTTPException
from starlette.middleware.base import BaseHTTPMiddleware


class RateLimitMiddleware(BaseHTTPMiddleware):
    """API速率限制"""
    
    def __init__(self, app, requests_per_minute: int = 60):
        super().__init__(app)
        self.requests_per_minute = requests_per_minute
        self.requests: Dict[str, list] = {}
    
    async def dispatch(self, request: Request, call_next):
        client_ip = request.client.host
        
        current_time = time.time()
        minute_ago = current_time - 60
        
        if client_ip in self.requests:
            self.requests[client_ip] = [
                t for t in self.requests[client_ip] if t > minute_ago
            ]
        else:
            self.requests[client_ip] = []
        
        if len(self.requests[client_ip]) >= self.requests_per_minute:
            raise HTTPException(
                status_code=429,
                detail="Too many requests. Please try again later."
            )
        
        self.requests[client_ip].append(current_time)
        
        return await call_next(request)
```

### 数据安全

#### 数据库加密

```python
# backend/core/encryption.py
"""
数据加密工具
"""

from cryptography.fernet import Fernet
from cryptography.hazmat.primitives import hashes
from cryptography.hazmat.primitives.kdf.pbkdf2 import PBKDF2HMAC
import base64
import os


class DataEncryption:
    """数据加密类"""
    
    def __init__(self, password: str):
        """初始化加密器"""
        salt = os.getenv("ENCRYPTION_SALT", "default-salt-change-in-production").encode()
        kdf = PBKDF2HMAC(
            algorithm=hashes.SHA256(),
            length=32,
            salt=salt,
            iterations=100000,
        )
        key = base64.urlsafe_b64encode(kdf.derive(password.encode()))
        self.fernet = Fernet(key)
    
    def encrypt(self, data: str) -> str:
        """加密数据"""
        return self.fernet.encrypt(data.encode()).decode()
    
    def decrypt(self, encrypted_data: str) -> str:
        """解密数据"""
        return self.fernet.decrypt(encrypted_data.encode()).decode()
```

#### 敏感配置保护

```python
# backend/config/secrets.py
"""
敏感配置管理
"""

import os
from typing import Optional


def get_secret(key: str, default: Optional[str] = None) -> str:
    """
    从环境变量或文件获取敏感配置
    
    优先级：
    1. 环境变量 {KEY}
    2. 环境变量 {KEY}_FILE 指向的文件内容
    3. 默认值
    """
    value = os.getenv(key)
    if value:
        return value
    
    file_path = os.getenv(f"{key}_FILE")
    if file_path and os.path.exists(file_path):
        with open(file_path, "r") as f:
            return f.read().strip()
    
    if default is not None:
        return default
    
    raise ValueError(f"Secret {key} not found in environment or file")


SECRET_KEY = get_secret("SECRET_KEY")
DATABASE_PASSWORD = get_secret("DATABASE_PASSWORD", default="")
```

---

## 数据库维护

### 数据库迁移

```bash
# 运行迁移脚本
cd backend
python migrations/add_calibration_logs_configs.py
python migrations/add_constraints_indexes.py
python migrations/optimize_indexes.py
```

### 数据备份

```bash
#!/bin/bash
# scripts/backup.sh
# 数据备份脚本

BACKUP_DIR="/backup/cauc-sep"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p "$BACKUP_DIR/$DATE"

# SQLite备份
cp /app/data/experiments.db "$BACKUP_DIR/$DATE/experiments.db"

# HDF5数据备份
tar -czf "$BACKUP_DIR/$DATE/data.tar.gz" -C /app data/

# 配置备份
tar -czf "$BACKUP_DIR/$DATE/config.tar.gz" -C /app config/

# 清理30天前的备份
find "$BACKUP_DIR" -type d -mtime +30 -exec rm -rf {} +

echo "Backup completed: $BACKUP_DIR/$DATE"
```

### 数据恢复

```bash
#!/bin/bash
# scripts/restore.sh
# 数据恢复脚本

BACKUP_DIR=$1

if [ -z "$BACKUP_DIR" ]; then
    echo "Usage: ./restore.sh <backup_directory>"
    exit 1
fi

# 停止服务
systemctl stop cauc-sep

# 恢复SQLite
cp "$BACKUP_DIR/experiments.db" /app/data/experiments.db

# 恢复HDF5数据
tar -xzf "$BACKUP_DIR/data.tar.gz" -C /app

# 恢复配置
tar -xzf "$BACKUP_DIR/config.tar.gz" -C /app

# 启动服务
systemctl start cauc-sep

echo "Restore completed from: $BACKUP_DIR"
```

---

## 常见问题与解决方案

### Q1: 后端服务响应慢？

**排查步骤**:
1. 检查CPU和内存使用率
2. 检查数据库查询性能
3. 检查网络延迟

**解决方案**:
```bash
# 增加Worker数量
# 修改 gunicorn.conf.py 中的 workers 参数

# 优化数据库索引
python migrations/optimize_indexes.py
```

### Q2: WebSocket 连接断开？

**排查步骤**:
1. 检查Nginx WebSocket代理配置
2. 检查后端WebSocket服务状态
3. 检查网络超时设置

**解决方案**:
```nginx
# 增加WebSocket超时时间
proxy_read_timeout 86400s;
proxy_send_timeout 86400s;
```

### Q3: 数据库锁定错误？

**原因**: SQLite并发写入限制

**解决方案**:
1. 减少并发写入
2. 使用WAL模式
3. 考虑迁移到PostgreSQL

```python
# 启用WAL模式
import sqlite3
conn = sqlite3.connect('experiments.db')
conn.execute('PRAGMA journal_mode=WAL')
```

### Q4: 内存占用过高？

**排查步骤**:
1. 检查内存泄漏
2. 检查缓存配置
3. 检查数据加载方式

**解决方案**:
```python
# 限制缓存大小
CACHE_MAX_SIZE = 1000

# 使用生成器处理大数据
def process_large_data():
    for chunk in data_generator():
        yield process_chunk(chunk)
```

---

## 相关文档

- [打包发布](./打包发布.md)
- [监控告警](./监控告警.md)
- [CAUC-SEP_技术文档.md](../../CAUC-SEP_技术文档.md)
